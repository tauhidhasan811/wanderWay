function navigateLogin(value) {
    if (value === "Admin/view/login.php") {
        window.location.href = value;  // Redirects to the selected value (URL)
    }
}

function navigateRegistration(value) {
    if (value === "Admin/view/admin_registration_page.php") {
        window.location.href = value;  // Redirects to the selected value (URL)
    }
}